# 논문 작성과 관리

이 문서는 VS CODE와 LaTeX를 사용하여 논문을 작성하고 git을 사용하여 버전 관리를 진행하는 방법을 제시함.

## VS CODE와 LaTeX를 이용한 논문 작성 기본 과정

- LaTeX의 기본 문법: [LaTeX 소개](./LaTeX/00.%20intro.md)
- VS CODE와 연동하여 빌드: [VS CODE와 연동](./LaTeX/01.%20with_CODE.md)를 참고

## git을 사용한 작성 논문 버전 관리

하나의 repogitory로 묶어서 논문 작성을 관리하되, 각 논문 사이의 독립성과 후속 연구의 비교를 위하여 아래 그림과 같은 형태로 repo를 구성하여 논문을 관리함

주요 아이디어로 하나의 repogitory에 각 논문을 작성하는 독립된 영역(orphan branch)를 작성하고, main branch에 submodule로 연결하여 쉽게 관리하는 것.

```bash
  # main branch structure
  paper_bag
  ├── .git
  ├── paper_tempalte # submodule
  │   ├── LaTeX
  │   │   └── ...
  │   ├── resource
  │   │   └── ...
  │   └── ...
  ├── paper_00 # submodule
  │   └── ...
  ├── paper_01 # submodule
  │   └── ...
  └── .gitmodules
```

### 신규 연구 논문 작성의 경우

논문 tempalte branch의 내용을 복제하여, 새로운 branch를 생성하고, 이를 새로운 submodule로 추가함

```bash
# 신규 연구 논문을 위한 새로운 branch 생성
cd paper_tempalte

## 현재 branch 확인 -> 논문 template 용 branch인지 확인
## 아닐경우 먼저 branch 변경

git checkout --orphan {신규 논문 구분 이름}
git commit -m "[INIT] 신규 논문 작성"
git push origin {신규 논문 구분 이름}

# 논문 template 용 branch로 변경
git checkout {논문 template branch 명}

# submodule 연결
cd ../
git submodule add -b {신규 논문 구분 이름} {논문 관리 원격 repo 주소} {신규 논문 구분 이름}
```
