# 브랜치(Branch) 관리

## 브랜치 생성

새로운 브랜치를 생성하여 기존 코드에 영향을 주지 않고 독립적인 개발을 시작할 수 있습니다.

```bash
# 새로운 브랜치 생성
git branch <새_브랜치_이름>

# 예시: feature/login 이라는 이름의 브랜치 생성
git branch feature/login
```

## 브랜치 목록 확인 및 검색

현재 저장소에 있는 모든 브랜치를 확인하거나 특정 패턴과 일치하는 브랜치를 검색할 수 있습니다.

```bash
# 로컬 브랜치 목록 확인
git branch

# 원격 브랜치까지 포함한 모든 브랜치 목록 확인
git branch -a

# 특정 패턴으로 브랜치 검색 (예: 'feature'로 시작하는 브랜치)
git branch --list 'feature/*'
```

## 브랜치 이동 (Checkout)

다른 브랜치로 작업 공간을 전환합니다.

```bash
# 기존에 생성된 브랜치로 이동
git checkout <브랜치_이름>

# 예시: feature/login 브랜치로 이동
git checkout feature/login
```

브랜치 생성과 이동을 한 번에 실행할 수도 있습니다.

```bash
# 브랜치를 생성하면서 해당 브랜치로 바로 이동
git checkout -b <새_브랜치_이름>

# 예시: 'feature/signup' 브랜치를 생성하고 즉시 이동
git checkout -b feature/signup
```

## 고아 브랜치 (Orphan Branch)

일반적인 브랜치는 기존 커밋 기록을 부모로부터 상속받지만, **고아 브랜치**는 부모 커밋 없이 완전히 새로운 히스토리를 시작하는 특별한 브랜치입니다. 마치 해당 저장소 안에서 새로운 `git init`을 실행하는 것과 같습니다.

### 고아 브랜치 생성

`--orphan` 옵션을 사용하여 고아 브랜치를 생성할 수 있습니다.

```bash
# 'new-history'라는 이름의 고아 브랜치 생성 및 이동
git checkout --orphan new-history
```

이 명령을 실행하면 `new-history` 브랜치로 이동하지만, 기존의 커밋 히스토리는 따라오지 않습니다. 이 브랜치에서 만드는 첫 번째 커밋은 부모가 없는, 완전히 새로운 역사의 시작점이 됩니다.

### 일반 브랜치와의 비교

| 구분 | 일반 브랜치 | 고아 브랜치 |
| --- | --- | --- |
| **부모 커밋** | 있음 (생성 시점의 커밋을 상속) | 없음 (완전히 새로운 시작) |
| **히스토리** | 기존 히스토리에 연결됨 | 완전히 분리된 독립적인 히스토리 |
| **주요 용도** | 기능 개발, 버그 수정 등 | 문서(gh-pages), 별도 프로젝트 관리 등 |

### 사용 사례

*   **GitHub Pages용 브랜치**: 프로젝트 소스 코드의 히스토리와는 무관하게, 웹사이트 빌드 결과물만 관리하는 `gh-pages` 브랜치를 만들 때 유용합니다.
*   **완전히 다른 프로젝트 관리**: 하나의 저장소 안에서 서로 관련 없는 여러 프로젝트의 히스토리를 분리하여 관리하고 싶을 때 사용할 수 있습니다.
