# Zotero와 LaTeX 연동 워크플로우 가이드

이 문서는 Zotero를 이용한 서지 정보 관리와 VS Code 기반의 LaTeX를 활용한 문서 작성을 연동하여, 연구 생산성을 극대화하는 전체 워크플로우를 요약합니다.

## 1. 기본 철학 및 도구 선택

- **역할 분리**: 정보 관리(Zotero)와 문서 작성(LaTeX)의 역할을 명확히 분리합니다.
- **전문 도구 사용**: 각 작업에 가장 전문적인 도구를 사용하여 효율성을 극대화합니다.

### 도구
- **서지 관리 (Zotero)**: 무료 오픈소스이며, 웹 자료 수집, 체계적인 관리, 강력한 플러그인을 통한 확장성이 뛰어납니다.
- **문서 작성 (LaTeX in VS Code)**: 전문적인 수식 조판, 내용과 서식의 분리, 대용량 문서의 안정성을 보장하며, LaTeX Workshop 확장 프로그램을 통해 현대적인 편집 환경을 구축할 수 있습니다.

---

## 2. 핵심 구성 요소 및 설정

효율적인 워크플로우를 위해 Zotero와 VS Code에 각각 필요한 플러그인과 확장 프로그램을 설정합니다.

### A. Zotero 설정
1.  **Zotero 설치**: [Zotero 공식 홈페이지](https://www.zotero.org/)에서 설치합니다.
2.  **필수 플러그인 설치**:
    *   **Better BibTeX (BBT)**: 예측 가능하고 고유한 인용 키(Citation Key)를 자동 생성하고, Zotero 라이브러리 변경 시 `.bib` 파일을 자동으로 업데이트하는 핵심 플러그인입니다.
    *   **Mdnotes**: Zotero의 각 서지 정보 항목에 연결되는 Markdown 형식(`.md`)의 요약/노트 파일을 생성하고 관리해주는 플러그인입니다.

### B. VS Code 설정
1.  **VS Code 설치**: [VS Code 공식 홈페이지](https://code.visualstudio.com/)에서 설치합니다.
2.  **필수 확장 프로그램 설치**:
    *   **LaTeX Workshop**: VS Code 내에서 LaTeX 문서를 작성, 컴파일, 미리보기 할 수 있는 통합 환경을 제공합니다. 특히, `.bib` 파일을 자동으로 인식하여 `\cite` 명령어 사용 시 인용 키를 자동 완성해주는 기능이 매우 강력합니다.

---

## 3. 데이터 관리 전략: 클라우드와 Git의 조합

Zotero의 무료 저장 공간(300MB) 한계를 극복하고 데이터를 안전하게 관리하기 위해, 데이터 종류에 따라 저장 방식을 분리합니다.

### A. 서지 정보 (메타데이터)
- **관리 방법**: Zotero Sync (클라우드)
- **설명**: 용량을 거의 차지하지 않는 서지 정보, 노트, 태그 등은 Zotero의 기본 동기화 기능을 사용하여 여러 기기에서 연동합니다.

### B. 첨부 파일 (PDF 등)
- **관리 방법**: Git 및 원격 저장소(GitHub 등)
- **설정**:
    1.  PC에 PDF 저장용 폴더를 만들고 Git 저장소로 초기화합니다.
    2.  Zotero 설정에서 이 폴더를 '연결된 첨부 파일 기본 디렉터리'로 지정합니다.
    3.  **주의**: Zotero의 데이터 폴더 전체를 Git으로 관리하면 데이터베이스가 충돌할 수 있으므로, 반드시 첨부 파일만 분리하여 관리해야 합니다.

### C. 개인 요약 노트 (.md 파일)
- **관리 방법**: 클라우드 스토리지(Dropbox 등) 또는 Git
- **설명**: Mdnotes 플러그인이 생성하는 Markdown 파일들을 클라우드 폴더나 첨부파일용 Git 저장소 내 별도 폴더에 저장하여 동기화합니다.

---

## 4. 전체 워크플로우

수집부터 작성, 동기화까지 이어지는 전체 워크플로우입니다.

1.  **수집**: 웹에서 Zotero Connector를 클릭하여 서지 정보를 Zotero 라이브러리에 추가합니다.

2.  **정리 및 요약**:
    *   PDF 파일을 Git으로 관리하는 첨부파일 폴더에 저장한 후, Zotero로 드래그하여 서지 정보에 링크합니다.
    *   Zotero에서 논문에 태그를 추가하고 정리합니다.
    *   Mdnotes 플러그인을 사용하여 `.md` 요약 파일을 생성하고, VS Code에서 열어 내용을 작성합니다.

3.  **작성**:
    *   VS Code의 LaTeX Workshop 환경에서 `.tex` 파일을 작성합니다.
    *   본문에 인용이 필요할 때 `\cite{`를 입력하면, Better BibTeX이 자동으로 업데이트한 `.bib` 파일의 내용이 자동 완성 목록으로 나타납니다.

4.  **컴파일**: VS Code에서 컴파일을 실행하면, 본문에 인용된 모든 문헌이 지정된 스타일에 맞춰 참고문헌 목록으로 자동 생성됩니다.

5.  **동기화**:
    *   **서지 정보**: Zotero 동기화 버튼 클릭.
    *   **PDF 및 노트**: 터미널에서 `git add .`, `git commit -m "업데이트"`, `git push` 실행.

이 시스템을 통해 참고문헌 관리의 모든 과정이 반자동화되어, 연구자는 가장 중요한 작업인 '사고'와 '글쓰기'에 온전히 집중할 수 있습니다.
