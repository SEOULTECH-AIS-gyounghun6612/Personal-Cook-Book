# 커밋(Commit) 관리

## Git 저장소 초기화

프로젝트 디렉터리를 Git 저장소로 만들기 위해 초기화합니다. `.git`이라는 숨김 디렉터리가 생성되며, 여기에 모든 Git 관련 데이터가 저장됩니다.

```bash
# 현재 디렉터리를 Git 저장소로 초기화
git init
```

## 변경사항 커밋 (Commit)

파일의 변경사항을 로컬 저장소에 기록하는 과정입니다. 커밋은 `staging area`를 거쳐 이루어집니다.

1.  **`git add`**: 작업 디렉터리에서 변경된 파일 중 커밋에 포함할 파일들을 `staging area`로 추가합니다.
2.  **`git commit`**: `staging area`에 있는 파일들을 하나의 커밋으로 묶어 로컬 저장소에 영구적으로 기록합니다.

```bash
# 특정 파일(들)을 staging area에 추가
git add <파일_이름_1> <파일_이름_2>

# 변경된 모든 파일을 staging area에 추가
git add .

# staging area의 파일들을 커밋
git commit -m "여기에 커밋 메시지를 작성합니다"
```

## 커밋 취소 (Uncommit)

상황에 따라 여러 방법으로 커밋을 취소할 수 있습니다.

*   **`git reset`**: 이전 커밋으로 상태를 되돌립니다. `--soft`, `--mixed`, `--hard` 옵션에 따라 동작이 달라집니다.
    *   `--hard`: 가장 강력한 옵션으로, 해당 커밋 이후의 모든 변경사항을 완전히 삭제합니다. **(주의: 복구 불가)**

    ```bash
    # 가장 최근의 커밋을 취소하고, 해당 변경사항을 unstaged 상태로 되돌림
    git reset HEAD^

    # 특정 커밋으로 돌아가고, 이후의 모든 변경사항을 삭제 (위험)
    git reset --hard <커밋_해시>
    ```

*   **`git revert`**: 기존 커밋을 취소하는 새로운 커밋을 생성합니다. 이력을 남기기 때문에 협업 시 더 안전합니다.

    ```bash
    # 특정 커밋의 변경사항을 되돌리는 새로운 커밋 생성
    git revert <취소할_커밋_해시>
    ```

## 커밋 기록 확인 및 이동

*   **`git log`**: 현재 브랜치의 커밋 기록을 시간순으로 보여줍니다.

    ```bash
    # 기본 로그 출력
    git log

    # 각 커밋을 한 줄로 간략하게 표시
    git log --oneline

    # 브랜치와 그래프를 함께 표시
    git log --graph --all --oneline
    ```

*   **`git checkout`**: 특정 커밋 시점의 코드를 확인하고 싶을 때 사용합니다. 이 상태는 'detached HEAD'라고 불리며, 임시적인 상태입니다.

    ```bash
    # 특정 커밋 해시를 이용해 해당 시점으로 이동
    git checkout <커밋_해시>
    ```
